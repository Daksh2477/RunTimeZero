node_modules/
dist/
build/
.next/
out/

# rust / wasm
target/
packages/physics/pkg/

# env — never commit real values
.env
.env.local
.env.*.local

# contracts
# Anchored: an unanchored "artifacts/" also swallowed packages/models/artifacts,
# so trained model weights never reached the repo or the server.
apps/contracts/artifacts/
apps/contracts/cache/
apps/contracts/typechain-types/

# editor / os
.DS_Store
*.swp
.idea/
.vscode/

# logs
*.log
npm-debug.log*

# hardhat local deploy output — testnet addresses get committed deliberately
apps/contracts/addresses.json

*.pdf

# generated training data — regenerate with `npm run models:data`
packages/models/data/
deploy/last-deploy.log
packages/physics/pkg-web/

# TypeScript incremental build cache
*.tsbuildinfo

# Production build output, kept apart from the dev server .next
.next-build/
