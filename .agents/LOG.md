# Work log

Append one line when you finish something and push it. Newest at the bottom.
This is how the other agent knows what already exists without reading the diff.

---

- 2026-09-12T03:15Z · agent-a · Established two-agent protocol (`.agents/`). agent-b: read PROTOCOL.md, take ports 4001/3001 and database `algacarbon_b`.
- 2026-09-12T03:17Z · agent-a · Public simulator shipped: POST /simulate + /sim page. Runs the same WASM twin as the engine. 1 ha / 30 d returns net -₹4,996 — biomass and credits alone do not cover cost, which is the wastewater argument demonstrated rather than asserted.
- 2026-09-12T03:20Z · agent-a · Copernicus/Sentinel-2 ingestion shipped: `apps/api/src/ingest/sentinel.ts` + `npm run ingest:sentinel`. Statistical API (not raster downloads), NDCI evalscript on B04/B05, SCL cloud masking, token cached against the 429 rate limit. UNTESTED against the live API — needs COPERNICUS_CLIENT_ID/SECRET. Falls back cleanly to replay fixtures. Ponds under 40 m are skipped and reported.
- 2026-09-12T03:27Z · agent-a · Models shipped. CHANGED THE PLAN: dropped ONNX for JSON coefficients — onnxruntime-node is 100MB+ for a logistic regression and a depth-5 forest, and JSON weights are readable by a judge. Training is still real sklearn, offline. crash_classifier AUC 0.790, divergence_classifier 92.2% on 3 classes, ndci_biomass R2 0.181. That low R2 is correct: with sigma 0.45 observation noise NDCI genuinely cannot predict biomass, which is why we use it as a divergence detector. IMPORTANT: the learned NDCI interval (~1.3x) is TIGHTER than the published 2.4x, so infer.ts floors it at the literature figure — never let a model trained on our own simulator make us more confident than validated research.
- 2026-09-12T03:37Z · agent-a · Expense ledger + site economics page shipped (`npm run db:expenses`, /console/site/[id]). Costs are DERIVED from pond area and real harvest_records, not invented. Confirms the research: separation (harvest+dry) is 51% of cost at the facility vs 2% at the smallholder, because one runs a centrifuge and the other sun-dries. Cost lands ~INR 11,572/tonne CO2 (~\$139) against Brilliant Planet's \$850 desert pilot — the gap is wastewater siting.
- 2026-09-12T04:45Z · agent-a · Codex out; sole ownership. Reviewed and merged their frontend rewrite + backend hardening (17 contract tests now pass, up from 10). Fixed three defects they flagged: counts[] type error, dangling test:api script, and simulator economics double-counting fertiliser AND carbon revenue on the same biomass. Added `docs/ROADMAP.md` — vertical slices M1-M7. Key finding: contracts have ZERO call sites, so the credits half of the product is unreachable from the running app.
- 2026-09-12T11:40Z · agent-a · Expanded crash_classifier from 10 features to 19. The four probes were producing only levels, trends and means — which threw away the diurnal amplitude, the one leading indicator open-pond probes can actually give (a pond that stops swinging DO day-to-night has stopped photosynthesising, and that precedes visible density loss). Added do/ph/temp amplitude, OD volatility, depth, log area, season sin/cos, and hours-since-harvest. Training ponds now vary geometry so the model cannot memorise one shape. AUC 0.790 -> 0.843. `crashFeatures()` is now exported from advisory.ts and MUST stay in lockstep with windowFeatures() in make_dataset.ts — they are two copies of one contract and nothing throws if they drift. HANDOFF.md step 6 test vector updated to the new shape; the teammate would otherwise have hit a silently-wrong result.
- 2026-09-12T12:05Z · agent-a · Found why no trained model has ever reached the server: .gitignore had an unanchored `artifacts/` intended for Hardhat, which also matched packages/models/artifacts. The weights were never tracked, deploy.sh never runs models:train, so production has been running with modelsLoaded() false — every advisory has come from the rules alone, silently. Anchored the ignore to apps/contracts/ and committed the 292K of weights. Also worth knowing: HANDOFF.md tells the teammate to commit packages/models/ after retraining; under the old ignore their retrain would never have left their laptop.
- 2026-09-12T13:20Z · agent-a · FOUND AND FIXED A LABELLING BUG THAT INVALIDATED EVERY CRASH NUMBER SO FAR. make_dataset.ts labelled a crash as "observed optical density fell by a third over the next 48 h". A scheduled harvest removes 45% of the biomass, so EVERY HARVEST WAS LABELLED A CRASH — the model was substantially predicting the farm calendar, which is why hours_since_harvest carried the second-largest weight. Injecting PumpFailure made it worse: that fault scales observed OD by 0.7 with the culture perfectly healthy, inventing crashes that never happened. Labels now come from true standing biomass with harvest windows excluded. Positives fell 663 -> 233 (10.4% -> 3.6%): roughly two thirds of what we called crashes were harvests or sensor artefacts. AUC 0.843 -> 0.959, and that number now means something. Using truth for the LABEL is legitimate (it is what a simulator is for); it is never a feature, so DECISION #6 holds.
- 2026-09-12T13:25Z · agent-a · Energy meter is now a real observable end to end: Reading.energy_kwh in Rust (0.5 W/m2, same constant as the planner), WasmPond::inject_pump_failure, a slide switch on GPIO27 in the Wokwi diagram, energyKwh + mixerRunning in the MQTT payload, and energy_kwh_mean + mixing_uptime as features 20 and 21. Note honestly: the energy features do NOT rank high for crash prediction, and that is correct — a stopped paddlewheel is not a culture crash. Their value is disambiguating an OD drop, which is exactly the confusion that caused the labelling bug above.
- 2026-09-12T13:30Z · agent-a · Added scripts/check-feature-contract.mjs, wired into npm run typecheck. The crash feature list lives in four places (make_dataset.ts, load_atp3.py, advisory.ts crashFeatures, and the trained artifact) and nothing threw when they disagreed. It has now caught drift twice in one session. The artifact is allowed FEWER features than the runtime (train_all.py drops constant columns for real data) but never extra.
- 2026-09-12T15:10Z · agent-a · Reviewed both teammate branches. origin/real-atp3-ml (Daksh): REAL WORK, real ATP3 data, 1,091,261 raw rows down to 2,597 training rows, ROC-AUC 0.586, and reported honestly — the doc explicitly warns not to call 0.586 "58.6% accuracy". Two problems: (1) it branched before the feature expansion, so its load_atp3.py and train_all.py will conflict hard with mine; (2) 0.586 is close to chance. Not a failure of their work — real pond data is genuinely harder and ATP3 lacks the harvest log our labels need. origin/frontend (Chetan): a SEPARATE Lovable app under runtimezero-source/ — TanStack Router + Vite + 53 radix deps, running entirely on mockData.ts. No physics, no API, no WASM. Cannot merge wholesale without discarding the real engine. Its genuinely good idea is the ROLE SPLIT (farmer / investor / researcher / admin), which I have adopted.
- 2026-09-12T15:15Z · agent-a · Built /farm: the farmer view, deliberately with no chart of any kind. Order is verdict, action, cost of inaction. /console keeps the readings and trends for researchers and investors. Landing doors are now by audience rather than by task. Also built the animated 2D pond (pond-view.tsx + pond-draw.ts, ~9 KB, no dependency): culture colour tracks optical density and health, circulation streaks stop when the paddlewheel stops, draggable probes, day scrubber. /sim went 4 KB -> 7.18 KB; whole app is 113 KB first load. NOT VISUALLY VERIFIED — no browser available in this session, so the canvas has been typechecked and bundled but not seen.
- 2026-09-12T16:05Z · agent-a · Ported Chetan's UI/UX from origin/frontend into the real app rather than merging his branch. What came across: the oklch design tokens verbatim, the three typefaces (Space Grotesk / DM Sans / IBM Plex Mono, via next/font so they self-host), the dark navy sticky masthead with icon tabs and a role pill, the four-way audience nav (Farm / Market / Lab / Fleet), and the BigReading card — 2.25rem number, uppercase label, plain-language hint, status dot that pulses only when something is wrong. What did NOT come across: TanStack Router, Vite, 53 radix deps, recharts, and mockData.ts. Icons are eight inlined lucide-grid SVGs instead of lucide-react. Net cost: 0 new dependencies, app still ~106-113 KB first load. His design, our engine, real API.
- 2026-09-12T16:10Z · agent-a · Surfaced `latest` (ph, temp, DO, OD, mixing) on FleetPond, reusing the telemetry window getFleet already fetches for advisories — no extra query. IMPORTANT: mixing is boolean|null, not boolean. Seeded telemetry has energy_kwh NULL, and collapsing null into false put a red "Paddlewheel: Stopped" alarm on all seven ponds. null now renders as a grey "No meter" card in a new `unknown` band. A pond with no meter fitted is not a pond with a stopped mixer, and a screen that cries wolf on every site is worse than one that says nothing.
- 2026-09-12T16:12Z · agent-a · Local dev note: nothing loads the root .env — apps/api/src/db/client.ts just reads process.env.DATABASE_URL. It works under pm2 (ecosystem.config.cjs reads .env) but a bare `npm run api` gets "password authentication failed for user postgres". Start it with: set -a; . ./.env; set +a; npm run api
- 2026-09-12T17:05Z · agent-a · Merged Daksh's origin/real-atp3-ml into packages/models. Kept his real-world knowledge — the two ATP3 filenames, the exact column spellings (PondID / DateTime / pH / "Temp (C)" / "DO (mg.L)" / OD750), the instrumentation-to-operational join on (pond, date), and the multi-format date parser. None of that could be guessed without the dataset in hand. Changed two things. (1) HEADER upgraded from his 10 columns to the current 21; the feature set grew after he branched and check-feature-contract.mjs now enforces agreement. (2) His version averaged instrumentation to one point per pond-day, which threw away the intra-day dissolved-oxygen swing — the best leading indicator the probes give. The instrumentation file carries DateTime, so the swing is recoverable, and the merged loader keeps it: do_amplitude, ph_amplitude and temp_amplitude are now real measured values on real data rather than constants.
- 2026-09-12T17:08Z · agent-a · Found a labelling flaw in his loader while testing: it took the FIRST optical density reading in the lookahead window rather than the LOWEST. A pond falling 22% a day never trips the 33% test on day one, so a gradual crash was labelled healthy. On a synthetic fixture this was the difference between 0 and 5 positives out of 120. make_dataset.ts already used the minimum; the two now agree. Verified the whole real-data path end to end with a generated ATP3-shaped fixture (two ponds, four readings a day, sixty days): parser reads it, 5 constant columns are dropped with a message naming them, and training completes on the remaining 16. Fixture and crash_real.csv deleted afterwards; artifacts retrained on crash.csv and back to AUC 0.959 / 21 features / source crash.csv.
- 2026-09-12T19:40Z · agent-a · Added Tailwind v4 to apps/web so Chetan's Lovable pages can be copied across instead of redrawn. CASCADE TRAP worth knowing: unlayered CSS beats layered CSS regardless of specificity, so the existing `a { color: inherit }` reset silently overrode every Tailwind text utility — the landing page's primary button rendered dark-on-dark and was almost unreadable. Fixed by moving the legacy resets into @layer base. Any plain CSS added to globals.css from now on must go in a layer or it will beat utilities.
- 2026-09-12T19:45Z · agent-a · Composition model added to the physics (packages/physics/src/composition.rs, 6 tests). Nitrogen stress shifts algae from protein to storage lipid — replete ~60% protein / 8% lipid, starved and well lit ~25% / 35% — and lipid accumulation needs light, so starving a pond in monsoon overcast yields neither growth nor oil. Carbon fraction is now composition-dependent (lipid 77% C, protein 53%, carb 44%) rather than a flat 50%, which means a lipid-rich pond genuinely locks more CO2 per kg. Reading gained protein_frac / lipid_frac / carbohydrate_frac. Grades: food ≥55% protein, lipid-rich ≥25% lipid, feed ≥40% protein, else fertiliser.
- 2026-09-12T19:50Z · agent-a · apps/web/src/lib/pricing.ts: Indian farm-gate wholesale by grade (food ₹450-900/kg, feed ₹180-320, oil ₹90-140, fertiliser ₹8-18) with a 12-month seasonal forecast. Monsoon discount is real and about -12%. Bands widen 1.5%/month out because we have no price feed and a tight number twelve months ahead would be a fabrication. Deliberately wholesale not retail — quoting retail is the easiest way to make a project look viable when it is not.
- 2026-09-12T19:55Z · agent-a · Expansion figures are now magnitudes plus a direction flag, never signed. "Extra running cost: -₹4.2 L" for a shrinking site read as a charge rather than a saving. Also added projected annual profit, not just the delta, because an operator borrowing against it needs the absolute number. Sensors consolidated: one multiparameter sonde per pond (₹26k) plus node, not three separate instruments — nobody buys them separately and listing them that way nearly doubled quoted capex.
- 2026-09-12T20:00Z · agent-a · Ponds can now be added and DISABLED, never deleted (apps/api/src/routes/land.ts). A pond that produced credits in 2025 must still resolve in 2030 when someone audits them, so deletion is not offered at all. Disabling requires a stated reason because it lands in that audit trail. Geometry is validated: >12,000 m2 refuses (one paddlewheel cannot mix it), >60 cm refuses (self-shading), and sub-40 m width warns that satellite verification is unavailable.
- 2026-09-12T21:30Z · agent-a · THE SIMULATOR HAS BEEN DEAD AND THE CAUSE WAS NOT WHAT ANY OF US THOUGHT. /sim showed "Updating estimate…" and em-dashes forever. It was not the rAF scheduling (Codex had already fixed that), not the WASM (loads and runs fine — verified standalone: ph 9.15, lipid 0.080), and not runTwin (verified on a scratch page: harvest 271 kg). The cause is `await searchParams` in the /sim server component: on Next 15.5 that silently prevents the client subtree below it from hydrating. The markup renders, React never attaches, no error appears in the console, no error boundary fires, and every piece works in isolation. Found by bisecting a scratch page — async alone is fine, `await searchParams` breaks it. Fix: read the query string with useSearchParams inside a client component (components/sim-workspace.tsx) instead of on the server. If anyone moves that back to the server, check the simulator still produces numbers before committing.
- 2026-09-12T21:35Z · agent-a · Two things that cost real time and are now prevented. (1) `npm run build` while `next dev` is running corrupts the shared .next and the dev server then throws MODULE_NOT_FOUND for chunks that look like application bugs — hit twice. There is now `npm run build:web`, which builds into .next-build via NEXT_DIST_DIR. (2) live-simulator swallowed every failure into `setStatus('failed')` with no detail, so a broken model looked identical to a slow one. It now keeps the message, shows it, and logs it.
- 2026-09-12T22:30Z · agent-a · Layout widths were borrowed from ad-supported sites. `.wrap` capped at 1224px and `.home-shell` at 1168px, which reserves the outer thirds of a desktop for advertising we do not carry — tables and pond grids were scrolling inside a narrow well with empty space either side. Now 1800px / 1500px with prose capped at 62ch so body text stays readable. Console pages moved off Tailwind's max-w-7xl too.
- 2026-09-12T22:35Z · agent-a · /verify only ever resolved a single divergence check — the raw material, not the thing anyone holds. Added GET /verify/batch/:id and GET /verify/certificate/:id so a buyer can walk certificate → batch → report hash → the checks behind it → the evidence. Verified: Naroda batch resolves claimed 20,690 → credited 18,924, refused 1,766, 3 checks, 50 kg retired by Surat Textiles Pvt Ltd.
- 2026-09-12T22:40Z · agent-a · Built /farm/land — the UI for the pond add/disable API that had been backend-only. No delete button exists anywhere on it; taking a pond out of use requires a stated reason because that reason lands in the audit trail beside every credit the pond produced.
- 2026-09-12T23:05Z · agent-a · Removed framer-motion. Its `initial={{opacity:0}}` left the marketplace stat cards permanently invisible whenever the animation did not start — a blank gap where three live figures should be. Entrance animations are CSS now with `backwards` fill, never `both`, so they fail toward visible. Also deleted the duplicate credits table from /console/investor: it carried min-w-[640px] which forced a sideways scroll on a phone, and /console/market already owns trading as cards. One table fewer, one page clearer.
- 2026-09-13T00:20Z · agent-a · The mobile topbar was broken and it took five passes to find why, because the cause was SIX overlapping .main-nav / .masthead definitions accumulated across our incremental CSS appends — a stale white-background masthead from before the dark port, a `flex-wrap: wrap` in one media query, a full-width 3-column grid in another, a 2-column grid in a third, a `flex-basis: 100%`, and a duplicate .role-pill with margin-left:auto. Each fix looked like it did nothing because a later rule undid it. globals.css now has exactly one definition of each. LESSON: appending to a 900-line stylesheet does not compose; grep for the selector before adding a rule.
- 2026-09-13T00:25Z · agent-a · Masthead now lifts into a floating capsule past 28px of scroll — fixed, inset, fully rounded, backdrop-blurred, content-width. On phones the wordmark text and nav labels drop to icons so eight destinations fit one row. Icons were reused across destinations (two leaves, two charts, two flasks); each has its own now, which matters precisely because the labels are hidden. The role badge moved out of the scrolling nav — inside it, it slid off the edge.
- 2026-09-13T02:10Z · claude · THE API HAD BEEN 503 ON EVERY REQUEST AND THE CAUSE WAS THAT NOTHING EVER LOADED `.env`. No dotenv, no `--env-file` anywhere in the repo, so `process.env.DATABASE_URL` was undefined and `db/client.ts` fell through to its hardcoded `postgres:postgres@localhost` fallback — "password authentication failed for user postgres" on a machine where peer auth over the unix socket works fine. Fixed with `--env-file-if-exists` on the API scripts and the five root scripts that touch Postgres, weather or the chain. If you add a script that needs a credential, add the flag too.
- 2026-09-13T02:15Z · claude · Crash training now holds WHOLE PONDS out (GroupShuffleSplit on a new `pond_group` column). The 16 windows cut from one pond overlap, so a row-wise split left near-copies on both sides and the AUC measured memorisation. 100 of 400 ponds held out: AUC 0.969, and the meta now records the split and the test positive rate. `load_atp3.py` emits the column too so real data groups by pond. The feature-contract checker ignores bookkeeping columns rather than counting them as features.
- 2026-09-13T02:20Z · claude · Pond geometry is editable (`PATCH /land/ponds/:id`). It previously rejected `{"areaM2":1500}` with "active must be true or false" — the route only did retire/reactivate, so the land screen's area edit could never have worked. Partial bodies now merge, area stays derived from length × width and is refused if sent directly, and crossing the 40 m satellite threshold is called out in the response because it changes how that pond is verified for the rest of its life.
- 2026-09-13T02:25Z · claude · Site unit economics were dividing total cost by ON-CHAIN-ANCHORED carbon, which is zero at every site until an oracle key exists — so every operator's screen read as a total loss. Now returns verified / issued / anchored separately and computes cost per tonne against issued inventory with `costBasis` naming which was used. Naroda ₹11,572/t, Bhavnagar smallholder ₹63,033/t.
- 2026-09-13T02:40Z · claude · THREE BUGS IN THE SENTINEL INGEST, ALL INVISIBLE IN A CHART. (1) `resx: 10` against an EPSG:4326 bbox asks for ten-DEGREE pixels: every pond collapsed to `sampleCount: 1` and `cloudFraction` was exactly 0.000 on all 140 passes, so partial cloud could not be detected. (2) The bbox was a square of the pond's long side — RW-01 is 40 × 300 m, so ~87% of its "pond index" was surrounding land. (3) `P1D` aggregation was not midnight-aligned, so each ~05:30 UTC overpass landed in the interval that began the previous afternoon and every pass was stored A DAY EARLY — which is why the first thumbnail renders came back blank: the renderer asked Copernicus for a date with no scene. All three fixed, 140 real passes re-ingested, cloud fraction now varies 0–0.267.
- 2026-09-13T02:45Z · claude · Real Sentinel-2 thumbnails: `GET /ponds/:id/imagery` lists passes, `GET /ponds/:id/imagery/thumbnail?date=` renders true colour from the public scene. Fixture rows from `npm run replay` deliberately get `thumbnailUrl: null` — putting a genuine photograph next to a synthetic number is the one thing this product cannot do. Worth knowing: the scene at RW-01's coordinates is built-up Naroda, so real NDCI reads 0.02–0.06. The channel works; there is no pond there.
- 2026-09-13T02:50Z · claude · `POST /simulate/pond/:id` projects a real pond on its site's real forecast (Open-Meteo, no key). The twin has no biomass setter, so it is warm-started by stepping until its density matches the pond's last reading — 102 h for DP-1 — and the response says so, because a projection whose starting point you cannot see is a guess with a chart. Each day comes back labelled heat / cold / light / density with one sentence of cause. Nutrients are topped up once after the warm-up: growing the twin up to the measured density also drained its nitrogen, which is an artefact of how we got there, not a fact about an effluent-fed pond.
- 2026-09-13T03:00Z · claude · Accounts: username and password, scrypt and HS256 out of `node:crypto`, no new dependencies. Writes need a token; `/verify`, `/summary`, `GET /market`, `/weather` and the anonymous `POST /simulate` stay open because a claim nobody can check without an account is worthless. Operator read screens go private via `AUTH_ENFORCE_READS` once the console has a login screen. TWO TRAPS: this API runs `--experimental-strip-types`, so constructor parameter properties pass `tsc` and kill the process at boot; and scrypt above N=16384 needs an explicit `maxmem` or every hash throws "memory limit exceeded" and registration 500s with nothing useful in the response. Demo account `admin`/`admin` via `npm run db:account`.
- 2026-09-13T05:20Z · claude · The demo rig is controllable and its clock is honest. `npm run sim` now runs a local HTTP control surface on 127.0.0.1:4400 (`GET /state`, `POST /speed|pause|resume|fault|harvest`) so a pond can be broken on stage — mixer, crash or overstatement, wired to the twin's existing injections from the pond's current hour rather than hour zero. FOUR THINGS WERE WRONG AND ALL OF THEM WOULD HAVE SHOWN ON STAGE. (1) Feed cadence: the twin only replenishes nitrogen at harvest, so weekly harvests left every pond producing literally zero from day 5 to day 7 — but feeding daily swung it to 44.7 g/m²/day, which is 105% OF THE PHYSICS CEILING, i.e. the rig would publish claims our own verifier must refuse. Feeding every 4 days and cutting 40% every 5 lands at 20 g/m²/day and 46% of ceiling, peak density 1.4, which is a real Gujarat raceway. (2) Readings were timestamped on ARRIVAL, so a run at 200× filed a fortnight of production under two minutes of timestamps and every claim dwarfed the ceiling for its own window; telemetry now carries `observedAt` and the ingest refuses a time it cannot believe instead of quietly substituting now. (3) The rig outran the wall clock and then had everything rejected — it now backfills at `--speed` and drops to real time on catch-up. (4) It published one message per TICK, so at 200× a pond was sampled once per eight simulated days: totals were right and the diurnal DO swing was gone, which is the one leading indicator the crash model has. Now one message per simulated hour, default speed 4 (~28 msgs/s over seven ponds, 30-day replay in three minutes), and a 6.8 mg/L swing survives the round trip.
- 2026-09-13T05:25Z · claude · `POST /harvests/pond/:id` records a weighed harvest — wet mass and moisture, with dry mass a generated column, because those are the two numbers a weighbridge and a moisture meter actually produce. This is the independent channel that does not depend on weather: satellite passes are days apart and monsoon cloud hides a fortnight of them, and for the smallholder tier Sentinel-2 cannot resolve the pond at all. The rig reports its own harvests through this endpoint over HTTP as an operator would, not by writing to the database, so it holds no privilege the product claims nobody needs.
