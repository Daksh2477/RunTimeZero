# @rtz/chain

Contract ABIs and typed ethers bindings. **Owner: Mahit.**

Generated from `apps/contracts` and **committed**, so `apps/api` and `apps/web` don't need a
Hardhat install to build.

Regenerate after any contract change:

```bash
npm run build --workspace=apps/contracts
npm run sync:abi
```
