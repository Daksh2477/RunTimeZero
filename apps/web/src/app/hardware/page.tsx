import { HardwareWorkspace } from '@/components/hardware-workspace';
export const metadata={title:'Simulated sensor hardware',description:'Explore sensor channels, wiring, pin signals and itemised Indian-rupee costs.'};
export default function HardwarePage() {
 return <main className="wrap hardware"><div className="page-heading"><div><p className="eyebrow">SIMULATED SENSOR HARDWARE</p><h1>A pond reading, traced back to its pin.</h1><p>Explore an electronic model of the sensor node: its channels, wiring, power and component costs. Every node in this demo is simulated.</p></div></div><section className="card" style={{padding:'1rem',marginBottom:'1.5rem'}}><h2 style={{marginTop:0}}>Circuit diagram: one device per pond</h2><p>Every sensor on one ESP32 node, its power chain, and the signed path from device to dashboard.</p><img src="/circuit/node-industrial.png" alt="Sensor node circuit diagram: ESP32 with pH, dissolved oxygen, temperature, optical density, paddlewheel and energy inputs, solar power chain, and the MQTT path to the dashboard" style={{width:'100%',height:'auto',borderRadius:12,border:'1px solid var(--line, #dde5e1)'}}/><p style={{display:'flex',gap:'1rem',flexWrap:'wrap'}}>{['basic','standard','industrial'].map(k=><a key={k} className="button secondary" href={`/circuit/node-${k}.png`} target="_blank" rel="noreferrer">{k} kit PNG</a>)}<a className="button secondary" href="/circuit/node-industrial.svg" target="_blank" rel="noreferrer">SVG</a></p></section><HardwareWorkspace/></main>;
}
