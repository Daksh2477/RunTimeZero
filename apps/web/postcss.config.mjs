/**
 * Tailwind v4 needs no config file — the design tokens live in the
 * @theme block in globals.css, exactly as Chetan's Lovable export had them.
 */
export default { plugins: { '@tailwindcss/postcss': {} } };
